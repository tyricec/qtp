# QTP (Quick Trip Planner) #

QTP is a web application to get trip information from point A to B.

The application uses Google's Directions API for trip information.

To run application, use the command:

```
npm run serve
```
